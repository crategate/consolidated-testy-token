[package]
name = "amm"
version = "0.1.0"
description = "OTC bulk AFHO deals. Vesting & buybacks."
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "amm"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build", "switchboard-on-demand/idl-build", "staking/idl-build"]


[dependencies]
anchor-lang = { version = "0.31.1", features = ["init-if-needed"]}
anchor-spl = {version = "0.31.1", features = ["token_2022"]}
bytemuck = { version = "1", features = ["derive"] }
switchboard-on-demand = { version = "=0.10.3", features = ["anchor", "devnet"] }
mock-dex-pool = { path = "../mock-dex-pool", features = ["cpi"] }
staking = { path = "../staking", features = ["cpi"] }