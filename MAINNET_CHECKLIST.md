# AFHO Mainnet Checklist

Everything required to go from the current devnet build to a mainnet launch, in rough dependency order. Source: full codebase audit (2026-08-22) + AGENTS.md gotchas + subsequent fixes.

## 2. Remove devnet-only code — §3/§4 landed; removal blocked only on migrating localnet tests off the mock fallback

- [ ] `programs/mock-dex-pool` (permissionless AFHO faucet — **fatal if shipped**).
- [ ] `load_test_data` + `load_offers` + `reset_devnet_state` (amm, incl. `scripts/amm-offers.ts` and `scripts/reset-devnet.ts`), `test_set_state` (crank-oracle), `update_amm_program` (staking).
- [ ] Permissionless `set_price` mock oracle PDAs.
- [ ] `test_set_state` has **no on-chain gate at all** (no signer, no state bound) — delete all test set states and dev toolste.
- [ ] **Devnet-only keeper modes & scripts.** Run the keeper for mainnet from `scripts/oracle/mev-keeper-mainnet.ts` (production-only copy: no `--test-state`/`test_set_state`/`test_collect_bounty`, no devnet genesis gate). These never ship to a mainnet run — delete or ignore them at launch:
    - `bount-test` / `bount-test-watch` Anchor scripts + the whole `--test-state` machinery in `scripts/oracle/mev-keeper.ts` (watch/cycle modes, `TEST_STATE_SEQUENCE`, devnet genesis check).
    - `scripts/oracle/set-oracle-state.ts` and the keeper's `--test-state` flag (drives `test_set_state`).
    - `scripts/rebalance-sol-pool.ts` (`anchor run rebalance-sol-pool`) — devnet-only arb stand-in; **never** point it at the mainnet canonical pool.
    - `scripts/amm-test-data.ts` / `amm-offers.ts` (drive the devnet-only `load_test_data` / `load_offers` instructions) and `migrate_offer_list` (devnet resize tool).
    - `scripts/reset-devnet.ts` (`anchor run reset-devnet`) — devnet/localnet full reset of the market-status clock (state 99 / day 0 / ts 0), the AMM runtime history (via `reset_devnet_state`: metrics/accepted_offers/offer_list/bookkeeping), and the `app/public/records.json` ledger + `records/` archives. Remove it, its `reset-devnet` Anchor label, and the `reset_devnet_state` instruction.
- Note: the mock fallback in `execute_swap` + the vestigial `sol_*` accounts (§4) are what localnet tests still depend on — delete them together in one pass.

## 3. Real price oracles — TWAP wired (devnet-verifiable), mock kept as localnet-only fallback

- [x] **Raydium CPMM TWAP reader** (`programs/amm/src/instructions/raydium.rs::read_twap_token0_in_token1`) — reads the `observation` ring and computes the time-weighted token_0/token_1 price (Q32.32). Fixed the ring layout (the account has an 8-byte anchor discriminator the reader originally skipped) and added a window-density guard (sparse rings fall back to vault ratio instead of returning a stale long-window TWAP).
- [x] `spot_oracle` → CPMM pool **TWAP** (`observation_state`) for claims, floor decay, dip trigger, buyback band. `read_cpmm_price_floor` uses TWAP when the ring is fresh+dense, else the instantaneous pool vault ratio; mock raw-u64 PDA remains the fallback only when `cpmm_pool_state` is unpinned (localnet tests).
- [x] `sol_oracle` → Raydium SOL/USDC pool (same reader, base=wSOL). Used by `offer_claim_sol` (mock `sol_oracle` remains the unpinned fallback). `bounty_top_up` and the crank's USD bounty read the SOL/USDC pool directly (`read_cpmm_price_floor` / vault ratio) and require the pool pinned — no mock fallback. MAINNET: a deep canonical pool is still required (claim reverts when the pool can't deliver ≥98% of the oracle-priced cost).
- [x] `priceChange24h` (momentum) → the close→close sample in `update_tradeday_stats` now reads the pool TWAP (via the same `read_cpmm_price_floor`), so momentum is self-sampled from the pool price, not Jupiter. **Revisit `calculate_momentum_score` weighting once the pool is live** (see §9).
- [x] Staleness/validity checks — fail-closed on missing/zero price; TWAP freshness gate (`TWAP_MAX_AGE_SECONDS`) + window-density guard; pinned-pool PDA verification (H1 re-pin).

## 4. Real DEX integration — Raydium CPMM, USDC leg live (Path A raw CPI)

- [x] **Rewrite `execute_swap` via raw `invoke_signed`** (not a typed Anchor CPI — the modern `raydium-cpmm-cpi` crate pins anchor 1.0, incompatible with our 0.31). The `raydium.rs` module builds the exact `swap_base_input` instruction (discriminator + 13 accounts) + PDAs. USDC leg routed when `cpmm_pool_state` is pinned; mock is the fallback.
- [x] **Pin the CPMM in state** — `AmmState.cpmm_program` / `cpmm_pool_state` / `cpmm_amm_config`, set via `set_cpmm_pool` (3 args, authority||keeper) + `scripts/set-cpmm-pool.ts`. Keeper derives the real vault/observation/authority PDAs.
- [x] **H1 re-pin for CPMM** — the CPMM accounts were `UncheckedAccount` (validated only by the CPI); now gated-verified against the derived vault/observation/authority PDAs whenever the pool is pinned (`pinned_pool_accounts_valid` / `pinned_sol_usdc_accounts_valid` / `require_pinned_pricing_accounts`), so a compromised keeper can't redirect the pricing reads or swap in/out vaults. No-op in mock/localnet mode.
- [x] **All-USDC claim conversion + SOL-leg retirement.** `offer_claim_sol` wraps the buyer's SOL → `swap_base_input` on the SOL/USDC pool → splits USDC 80/10/10 into `usdc_vault`/`usdc_dip`/`usdc_rewards`. Buyer covers the CPMM 0.25% input fee (+25bps on the lamports); min-out = 98% of cost (2% tolerance for pool drift/slippage — never binds on a deep mainnet pool). `wsol_vault` ATA is created idempotently on each claim (and in `bounty_top_up`) because the top-up closes it after unwrapping. The SOL swap legs in `dex_buyback`/`buy_the_dip`/`distribute_staker_rewards` are removed (USDC-only); `execute_swap` simplified; `set_sol_usdc_pool` + `cpmm_sol_usdc_pool`/`cpmm_sol_usdc_config` added.
- [ ] Remove the now-dead `sol_vault`/`sol_dip`/`sol_rewards`/`sol_oracle` state fields + accounts (left in place this pass to limit churn).
- [x] **SOL/USDC pool provisioning.** `scripts/set-sol-usdc-pool.ts` pins the pool: env vars (`DEVNET_SOL_USDC_POOL`/`DEVNET_SOL_USDC_CONFIG`) → deployment.json → devnet fallback that creates its own SOL/USDC CPMM pool seeded at 200 USDC/SOL (parity with the mock `sol_oracle`), writing `raydiumSolUsdcPool`/`raydiumSolUsdcConfig`. MAINNET: set the env vars to the canonical Raydium SOL/USDC pool — see §7.
- [ ] Devnet runtime-verify the SOL claim end-to-end (pool created + pinned now; claim swap + wSOL wrap still need a live run).
- [x] **`minimum_amount_out`** from the spot oracle (front-line M3 band) inside the CPI — done for the USDC leg.
- [x] **USDC-denominated exec price for the ratchet** — post-swap `(usdc_raw × 1e6)/afho_raw` reload + `ratchet_within_band` (unchanged).

## 5. Liquidity pool (Raydium CPMM — Token-2022 compatible)

- [x] **Programmatic pool init.** `scripts/create-pool.ts` (split from the old `mint-launch.ts`, 2026-09-09) creates the devnet CPMM pool via `raydium.cpmm.createPool` and writes `raydiumPool`/`raydiumAmmConfig`/`raydiumProgram`/`raydiumLpMint` to `deployment.json` (`USDC_MINT` env for mainnet). Replaces the old "manual UI" plan.
- [ ] **Launch split 25% LP / 75% protocol — DONE in code (2026-09-09), pending mainnet run.** `fund-launch.ts` mints the supply directly into the final destinations in ONE tx: 75% → `afho_vault`, 25% → the pool-seed ATA, then revokes mint authority (metadata already immutable since `mint-create`) — the authority wallet never holds supply (no top-holder concentration snapshot for screeners). **Where the numbers live:** `fund-launch.ts` (`LAUNCH_TOTAL_SUPPLY`, `LP_SHARE_PCT`) and `create-pool.ts` (`AFHO_TO_LP`/`USDC_TO_LP`; both sides seed from the seed ATA — the vault is program-custodied and never touches the pool).
- [ ] **1% of bond sales → LP until target.** 4th split leg in `offer_claim` routing 1% of proceeds to an LP-funding vault + a permissionless `lp_fund` instruction that CPI-`addLiquidity` until pool liquidity ≥ target. **Target: $100,000** (sanity: at 0.25% fee this is enough to make swap depth / TWAP meaningful; revisit after launch volume).
- [ ] **LP custody.** Burn vs lock LP tokens (Raydium `cpmm.lockLiquidity` supports locking). Protocol-owned PDA affects "target size" measurement + withdrawal risk. `scripts/burn-lp.ts` implements burn-all (dry-run by default, `EXECUTE=1`); keep LP during the Phase-0 rehearsal (recoverable), burn at real launch once the pool is at final size (top up via `addLiquidity` BEFORE burning — later deposits mint fresh LP that should be burned too).
- [ ] **Mainnet pool conversion (devnet shapes → mainnet).** Two pools, two different strategies:
    - **AFHO/USDC — created BY `create-pool.ts`** (`AFHO_TO_LP`/`USDC_TO_LP` env, defaults 250M AFHO : 1250 USDC = $5e-6). On mainnet this pool IS the AFHO market — there is no external price to rebalance to; its vault ratio just is the price, so the seed sets the launch price (devnet seeded 250M AFHO : 1250 USDC = $5e-6). Depth matters for `bounty_top_up` leg 1 and the buyback slices. CPMM pool addresses are PDAs of (amm_config, mint pair), so a deeper pool for the same pair can never be re-created — seed the depth in at launch, or deposit later at ratio (`raydium.cpmm.addLiquidity`).
    - **SOL/USDC — pin, don't create.** Point `DEVNET_SOL_USDC_POOL` / `DEVNET_SOL_USDC_CONFIG` (historical names — they are the mainnet override path) at the deep canonical wSOL/USDC pool, chosen so its config has creator fee DISABLED (`feeOn = 0`, else the 25bps claim/top-up solve is wrong) and its USDC is real USDC (swap the mint constant in `set-sol-usdc-pool.ts` / `init-bounty.ts`). NEVER run `rebalance-sol-pool` against it — that script is the devnet arbitrage stand-in only.
    - **Depth rule for `bounty_top_up`** (10 bounty payments ≈ $7.50 per top-up, USDC-priced): a single buy must stay ≤ ~5% of the pool's wSOL side for ≤5% impact → at a $200 SOL price the top-up buys ~0.0375 SOL, satisfied by any pool deeper than ~$1.50 of wSOL. The canonical pool satisfies this trivially; devnet's 0.3-SOL seed can never fill a large one — fix with `anchor run rebalance-sol-pool` (re-anchor to 200) plus a manual at-ratio liquidity deposit (Raydium UI or `raydium.cpmm.addLiquidity`).

## 6. Bounty / keeper / treasury

- [x] Rotate keeper off authority via `set_keeper` (H1 urgency reduced post-pin, still do it).
- [x] Bounty vault rent floor + setters (L5).
- [x] Bounty pays only on state transitions (rate ~2/day).
- [x] **Bounty auto-top-up implemented (`bounty_top_up`, permissionless).** Sized in bounty PAYMENTS (2026-09-10, was 0.2/0.4 SOL): when `bounty_vault`'s USDC value (at the pinned SOL/USDC pool price) is ≤ 10 × `bounty_usd`, sell AFHO from the treasury `afho_vault` → USDC (AFHO/USDC pool) → wSOL (SOL/USDC pool) → unwrap into `bounty_vault`, adding **10 payments worth (BY, not TO; ≈ $7.50 at the $0.75 bounty)**. `bounty_usd` is read from the crank's `BountyConfig` PDA (constrained under the pinned crank program; `bountyConfig` is a required account now). Two pool hops, one atomic instruction (the intermediate USDC passes through `usdc_vault` net-zero within the same instruction). Keeper attempts it every loop.
- [ ] Devnet runtime-verify `bounty_top_up` (fund `afho_vault` with AFHO first, drain `bounty_vault` below the 10-payment low water, crank it, confirm it adds ~10 payments of SOL funded from AFHO). Run the keeper with `BOUNTY_RECYCLE=0` — recycling is on by default in production mode and would keep the vault funded. NOTE: the keeper's new `accountsStrict` includes `bountyConfig` and the sizing lives on-chain — `anchor build` + redeploy the amm program BEFORE running the updated keeper.
- [x] **USD-priced bounty + inflation.** `BountyConfig` now stores `bounty_usd_raw` (USDC raw, 6 dp) + `base_year` + `annual_inflation_bps`; `permissionless_crank` pays `lamports = usd_raw × 1e6 / sol_price` where `usd_raw` is the base amount compounded by the configured bps per calendar year since `base_year`. Defaults in `init-bounty.ts`: $0.75, base year 2026, +5%/yr. The SOL price is the pinned SOL/USDC pool vault ratio; falls back to the legacy fixed-lamport `bounty_amount` when the pool isn't configured (the oracle never dies).
- [ ] Devnet runtime-verify the USD bounty (force a transition, confirm ~$0.75 worth of SOL lands).
- [x] **Keeper bounty recycling** (`BOUNTY_RECYCLE` env, mev-keeper). Default ON in production crank mode, OFF under `--test-state` (the harness deliberately drains the vault so the `bounty_top_up` refill loop runs for real). The keeper immediately re-funds the bounty vault with the bounty it just collected, so the vault stays above the 10-payment low water and AFHO-funded top-ups never fire while the operator's keeper is the only earner; the keeper nets ~0 minus the recycle tx fee. Flip it off (`BOUNTY_RECYCLE=0`) once third-party keepers win transitions — their collections drain the vault and top-ups resume as designed. Runtime-verify alongside the `bounty_top_up` item above.
- [ ] **Bounty USD conversion is POOL-priced** — `permissionless_crank` pays `usd_raw × 1e6 / pool_price`, so the payout's REAL value scales by market_price / pool_price. Devnet's drifted pool ($1,232/SOL) pays ~$0.09 of real value per $0.75 bounty; on mainnet (canonical pool ≈ market) it pays the real $0.75. Tune by hardcoding `BOUNTY_USD_RAW` in `scripts/init-bounty.ts` (USDC raw, 6 dp) before `anchor run init`, or post-init via the authority-gated `set_bounty_usd` crank-oracle instruction (now scripted as `anchor run set-bounty-usd`; the +5%/yr inflation compounds on whatever is set). $0.75/transition at ~2–6 transitions/day vs ~3–5 crank txs + the keeper's data-API costs is the keeper-economics dial — revisit when a second keeper instance exists.


## 7. Ops / launch sequence

- [x] Staking issues fixed and verified in code: `amm_stake.rs` gate is the AMM-state PDA (not a program-ID is_signer), `Stake`/`CreateAmmPosition` use `8 + StakePosition::INIT_SPACE`; staking tests moved to `tests/staking.test.ts` with a working `crank_oracle` import. (Full test suite re-run pending — see below.)
- [ ] `current_stake_ratio` uses `total_supply` vs circulating (`helpers_make_offers.rs:31-33`) — resolve or accept.
- [x] Fresh `anchor build` + full test suite green (**34/34** local suites) — re-run after the 2026-08-31 re-audit fixes (S1–S4 + authority-only pool pinning).
- [x] **Devnet end-to-end rig** — `anchor run mint` (fresh mint + AFHO/USDC CPMM pool) → `anchor run amm-init` (now also initializes the **staking pool**) → `anchor run set-cpmm-pool` (pins program+pool+config) → `anchor run set-sol-usdc-pool` (pins/creates the SOL/USDC pool) → `anchor run bount` (keeper derives CPMM accounts). USDC legs live; SOL claim leg live on devnet pending a runtime claim test. (`scripts/pool-init.ts` still exists as a standalone staking backfill.)
- [ ] `amm-init` with real mint/pool/oracle addresses; verify `deployment.json` consumed by app.
- [ ] External audit pass on the final diff (§1 fixes + §4 adapter + §5 LP).
- [x] Doc cleanup: staking header (claim penalties removed), stale comment in `dex_buyback.rs` (SOL legs), `docs/DEX-INTEGRATION.md` (raw `invoke_signed`, no typed CPI) — done in the 2026-08-31 pass.

## 7b. Staged mainnet dry run (2026-09-07 plan — cheap, mostly refundable)

A full-fidelity mainnet rehearsal is possible with today's config switches, but mainnet is PUBLIC — treat it as a staged soft launch, not a dry run. Phases, cheapest first:

- [ ] **Phase 0 — deploy + pin, no oracle, no keeper.** Mainnet cluster config, deploy the 3 programs (~7 SOL rent, refundable — do NOT `--final`), `mint-create` → `amm-init` → `fund-launch` → `create-pool` with a small pool (`USDC_TO_LP=500`-style env + AFHO leg at the intended launch price), `set-cpmm-pool` + `set-sol-usdc-pool` (canonical pool), frontend against the mainnet `deployment.json`. Without a Switchboard feed the crank sentinel (state 99) keeps the desk CLOSED — nothing trades, nothing can be claimed. Validates deploy size/CU, pool creation, pins, ALT/v0 claim txs and wallet-sim behavior against real RPCs. **Recoverability: keep the LP tokens (skip §5 lock/burn) — burning them returns BOTH legs (AFHO + USDC); `solana program close` refunds the program rent while upgradeable.**
- [ ] **Phase 1 — feed + keeper, no desk.** Deploy the real mainnet Switchboard market-status feed (irrecoverable cost — queue/feed fees) and run the keeper without posting sheets (skip `make_offers` or run it and never announce). Exercises quotes, bounty USD math at real SOL prices, `bounty_top_up`, buyback/dip against the thin pool. **Real-money caveats:** every flow is real — ratchet `highest_buyback_basis` gets pinned by dry-run buyback fills and (same mint) would carry into launch; thin-pool slippage amplifies the §14 spot self-reference. Either accept the state history or use a fresh mint/pool for the real launch.
- [ ] **Phase 2 — desk live, no announcer.** Post sheets + test one claim of each currency (you are the counterparty; the 80/10/10 USDC lands in vaults that only exit through program flows, so keep test volume tiny). Twitter announcer stays OFF until launch.
- [ ] **Not recoverable (budget):** tx fees, Switchboard feed/queue costs, keeper RPC + EARNINGS API subscription, thin-pool slippage on test flows. Stranded PDA rent is small (biggest is `amm_state`); vault tokens exit via LP burn + the program's own flows only — no authority sweep exists.
- [ ] **Endgame choice:** (a) throw away — close programs, redeem LP, fresh mint at launch (cleanest state, re-pays ~10–12 SOL rent); or (b) keep — deepen the SAME CPMM pool at ratio (Raydium `addLiquidity`; pool PDAs are config+mint-derived so this pool IS the launch pool) and accept the rehearsal state history. Decide before posting sheets.

## 8. Token / program trust posture
- [x] Revoke **mint authority** (in `fund-launch.ts` — same tx as the supply mint); freeze authority already `null`.
- [x] Revoke **metadata update authority** (immutable name/symbol/URI) — `createUpdateAuthorityInstruction` (`newAuthority: null`) in `mint-create.ts`, same tx as the mint: immutable from reservation (2026-09-10, was fund-launch).
- [ ] **Program upgrade authorities** for amm/staking/crank-oracle → renounce or multisig. Not blocked — it's a deploy-side CLI step (needs the deployer keypair + a live program): `solana program set-upgrade-authority <PROGRAM_ID> --new-upgrade-authority 11111111111111111111111111111111` (burn address = immutability) or point at a multisig. Do at launch after the final audit-pass deploy.
- [x] **Burn LP tooling** — devnet 1-raw-LP smoke test superseded by `scripts/burn-lp.ts` (2026-09-09): burns 100% of the holder's LP, dry-run by default, `EXECUTE=1`. MAINNET custody decision (burn-all vs `cpmm.lockLiquidity`) still §5.
- [ ] No hidden mint path: confirm `mintTo` only via revoked mint authority.

## 9. Momentum metric soundness — rework to self-sampled pool price

- [x] **Source swap + sampling cadence.** Momentum now reads the **spot oracle's close→close change** (`record_price_change` computes `(close−prev)/prev × 10000` centi-percent into the same `price_changes[20]` ring), instead of Jupiter's `priceChange24h`. Cadence is per-trading-day (one close per day). `MarketMetrics.daily_close` holds the baseline.
- [x] Wire the spot oracle itself to the Raydium pool TWAP (see §3) — `update_tradeday_stats` now samples the pool TWAP (vault-ratio fallback) as the daily close.
- [ ] Re-tune bump-taper coefficients against `sim/` once the source is a live pool price (single venue, not JUP's aggregate).
- [ ] **Tracked edge:** `raw == 0` reads as "no sample", so a perfectly flat market doesn't count toward `MIN_SAMPLES=5` and the desk can read cold on a flat open. Design is otherwise sound and source-agnostic.

## 10. Switchboard → mainnet

- [x] **Status-only quote.** `feed-deploy.ts` now deploys just the market-status feed (JUP price feed removed); the keeper already falls back to status-only when `priceFeedId` is absent.
- [x] **Dual-source halt confirmation in the feed job (2026-09-10).** A single API reading "halted" no longer publishes state 3: the job re-runs the earnings chain and only confirms 3 when BOTH sources read halt; disagreement reports the SECOND source's honest value; secondary-down-while-primary-halts trusts the primary (tradeoff documented in feed-deploy.ts — flip `onFailureValue` to "0" to fail closed instead). Non-halt states stay single-source. Validated against 9 canned payload cases (tools probe, all pass). NOTE: job hash changes → new feed id + quote account; re-run `feed-deploy` and the keeper picks it up from deployment.json (the crank validates the quote against its own embedded feed ids, no program change).
- [ ] Redeploy feeds to the **mainnet default queue** (`A43DyUGA7s8eXPxqEjJY6EBu1KKbNgfxF8h17VAHn13w`) — `feed-deploy.ts` uses `getDefaultQueue(rpcEndpoint)`; confirm it resolves mainnet.
- [ ] Set mainnet feed job API keys (MASSIVE/EARNINGS) as `variableOverrides`.
- [ ] Mainnet oracle-program/quote-program IDs (see switchboard skill) vs devnet.

## 11. Frontend

- [x] Position card tint by market state (after-hours `#f7dec0`, closed/halted `#ddd6ff`).
- [x] Exit button shows per-state principal-penalty % (incl. 0% open).
- [ ] UI smoke test on devnet for claim/unstake + the new penalty label.
- [x] `useUnstake.ts` hardcodes `principalPenaltyBpsForState` (300/600/1800) for a console.log estimate while the exit button reads the pool's configured bps — thread the pool bps through so the debug log can't drift from the real penalty. (DONE 2026-09-07: `useUnstake` now takes the pool and derives the estimate from `afterHoursPenaltyBps`/`closedPenaltyBps`/`haltedPenaltyBps`, devnet defaults only as a pre-load fallback; `Positions.tsx` passes the pool.)
- [ ] `/dash` still renders the vestigial `posrVault` tile (`useDashData.ts`) — remove when the posr PDA is dropped.
- [x] SOL-claim buyer flow exists in the frontend (`useOfferClaim.ts` supports the `sol` payment currency → `offerClaimSol`); devnet runtime smoke test still pending above.
- [x] **SOL-claim wallet-sim failure fixed (2026-09-07).** Root cause: the tx was provably sound (484-byte v0, 26 ALT-resolved keys, ~162k/400k CU, lands, RPC sim clean) but Backpack preflights against its OWN RPC with a blockhash the app fetched at `confirmed` milliseconds earlier — a node a slot or two behind returns `BlockhashNotFound`, a bright red box on every claim. Fixes in `useOfferClaim.ts`: blockhashes now taken at `finalized` (per-tx fresh fetch for the 3-popup SOL flow); every tx (USDC + SOL) is dry-run in-app (`sigVerify:false, replaceRecentBlockhash:true`) immediately before its wallet prompt, so real failures (desk closed, sheet stale, floor held, lots gone, CU) surface as readable in-app errors with program logs BEFORE the wallet opens (RPC-flaked dry runs soft-skip); ALT fetch retries once before the legacy fallback. Error banners (`Claim failed` + RPC error) now auto-scroll into view. Prior attempts (fresh-state gates 073a0e6, exact SOL cost mirror + v0+ALT bafe727) were correct and kept — nothing needed reversing.
- [x] **App production build repaired (2026-09-07).** `program.account.offerList` on the generic `AccountNamespace<Idl>` broke `tsc -b` (and thus `yarn build`) since 073a0e6 — vite dev never typechecks, so it went unnoticed. Cast added; `cd app && yarn build` is clean again. GATE: run `yarn build` (not just the dev server) before any deploy — a red build must block shipping.
- [ ] **Backpack runtime verification of the SOL claim flow** — confirm no red simulation box and readable in-app errors on a real 3-tier claim (user's wallet; the fix is verified at the RPC level only).
- [ ] **Priority fees for mainnet congestion.** No transaction in the claim/keeper paths carries a compute-unit-price instruction (CU *limit* only). At NYSE open/close congestion an unprioritized keeper tx can miss the transition window and wallet claims can stall. Add a configurable `ComputeBudgetProgram.setComputeUnitPrice` to mev-keeper-mainnet's transition-critical txs and optionally to claim checkout before mainnet.

## 12. Deploy cost & program size — the ~690 KB `amm` is the whole ballgame

- [ ] **Never delete `target/deploy/*-keypair.json`.** Those keypairs *are* the program addresses. `rm -rf target` regenerates new IDs, so `anchor deploy` pays full rent for fresh program accounts. Iterate with `anchor upgrade <program>` against the same keypairs — an upgrade pays only the *delta* rent plus a transient buffer you close. Rent re-measured 2026-09-08 via `getMinimumBalanceForRentExemption`: 3 programs (amm 656,704 + staking 414,944 + crank_oracle 305,512 = 1,377,160 bytes of `.so`; mock-dex-pool is deleted) cost **~7.0 SOL at the reduced rate already live on devnet (~5,081 lamports/byte)** and **~8.7 SOL at mainnet's current rate (~6,334/byte — already 9% below the classic 6,960)**; the Agave v4.2 rent reduction brings mainnet down to the devnet rate. (The old "4 programs / ~11.5 SOL / 1.66 MB" figure predates the mock-dex-pool deletion AND the rent reductions.)
- [x] **Skip on-chain IDLs on mainnet (2026-09-09).** No pipeline step runs `anchor idl init` today — keep it that way: all scripts and the app read `target/idl/*.json` / `target/types/*` from disk, nothing fetches an on-chain IDL. Uploading all three would cost ~0.56 SOL of rent (amm 0.366 + crank 0.095 + staking 0.104) for zero functional benefit.
- [x] **Codegen flags landed (2026-09-09).** `panic = "abort"` workspace-wide + per-package `opt-level = "z"` for `crank-oracle`/`staking` (amm keeps `"s"` — stack-budget history). Measured: amm 656,704 → 653,408, staking 414,944 → 398,368, crank_oracle 305,512 → 298,968 (also dropped crank's unused `anchor-spl` dep — LTO had already dead-stripped it). Total −25.8 KB ≈ −0.13 SOL — fat-LTO dilutes per-crate flags, so the remaining lever is structural (crank native rewrite, ~−1.1 SOL; see docs/DEPLOY-COST-REDUCTION.md).
- [ ] **Recover stranded devnet rent.** The pre-rotation program IDs (see `git diff` on the four `declare_id!`s) still hold ~11.5 SOL of rent-exempt lamports. If truly orphaned, close them (`solana program close <OLD_ID> --bypass-warning`, devnet only, requires upgrade authority).
- [x] **Cut `amm`'s switchboard type dependency.** `make_offers` now types `price_oracle` as `UncheckedAccount` (`address = amm_state.price_oracle`) and `amm` has no switchboard dependency. Next step: drop the `price_oracle` account + `AmmState.price_oracle` field entirely (still pinned-but-unread).

## 14. Open findings from the 2026-08-31 re-audit

- [ ] **Keeper transition-race (robustness).** `mev-keeper.ts` fires `make_offers`/`calc_completed_offers` only when ITS OWN crank call flips the state; if another bot wins the permissionless crank race, the day-end sheet never posts from this keeper. The on-chain idempotency guards (`offer_list.day_index`, `accepted_offers.day_index`) make firing on any observed state change safe — decouple the trigger from the keeper's own crank result.
- [x] **`initialize_state` writes `current_state = 99`** (crank-oracle) — RESOLVED 2026-09-02 by documentation: the sentinel is intentional (fail-closed until the first real quote); documented in AGENTS.md gotchas. Clamp-to-valid rejected — a valid initial state would open gated flows before any oracle reading.
- [x] **`max_multiplier_bps` uncapped** in `staking::initialize_pool` — FIXED 2026-09-02: `require!(max_multiplier_bps <= 30_000, StakeError::InvalidBps)` beside the other bps caps (spec header claims 1.0x–3.0x).
- [x] **`create_amm_position` accepts index gaps** — FIXED 2026-09-02: `require!(index == user_index.next_index)` at the staking boundary (`StakeError::InvalidIndex` parity with `stake`). The AMM's `validate_user_index` already enforced it pre-CPI, so no behavior change for legitimate claims.
- [ ] **Spot self-reference when TWAP is stale.** The M3 band + claim pricing fall back to the pool's instantaneous vault ratio when the TWAP ring is stale/sparse — the read and the swap share the same pool. Acceptable on a deep pool (launch §5); re-check after the pool matures.
- [ ] **Permissionless-crank bounty griefing (accepted).** Any caller can burn tx fees to consume a transition unpaid (racing the keeper) — inherent to permissionless cranking; the 2026-08-31 fix closes the same-quote replay variant. Track only if keeper reliability suffers.



## 2026-09-07 pass — desk UX, pricing edge case, bounty, keeper colors
- [ ] **Devnet runtime-verify the state-1 floor hard-bound (rescue REMOVED 2026-09-08).** Seed a floor just above spot via `load_test_data`, hold state 1: the claim must revert `FloorHeldAtSpot` and the UI must show "Floor held" with NO late-nite bonus pill in the `floor ∈ [live, live + 0.5%×live)` band; state 2 with the same floor prices at `floor − 0.5%×live` with the bonus pill.
- [ ] **Audit findings from the 2026-09-07 full re-read.** In-session security re-read of the three live programs (solana-auditor-skill methodology; amm swap/claim/decay paths, crank-oracle bounty/crank math, staking vault pinning re-verified against the S1–S5 fixes): NO new Critical/High findings beyond what §1/§14 already track. Re-verified guards: keeper swap paths capped by pacing (`MIN_SLICE_SLOTS`/`DIP_MIN_SLICE_SLOTS`), the 5% `MAX_SLIPPAGE_BPS` band + `ratchet_within_band` on every fill, `buy_the_dip` 40% day cap (`DIP_DAY_CAP_BPS`), pinned-pool PDA validation (`pinned_pool_accounts_valid`/`pinned_sol_usdc_accounts_valid`) in `bounty_top_up`/`offer_claim_sol`, sheet ≤5% of vault (totals bps plateau 500), `make_offers` day_index idempotency, floor-decay depth/demand guards with unit tests (17 amm unit tests pass), bounty rent floor + checked math, `state <= 3` + quote-replay guard. The state-1 rescue added this pass was REMOVED 2026-09-08 (its on-chain branch was dead code — `bonus_stored` is always 0 in state 1 — while the UI mirror applied it; see the runtime-verify item above). Still-open items that remain launch-relevant: §14 keeper transition-race + TWAP-stale spot self-reference, §5 LP custody/fund + 25/75 split, §2 devnet-code removal, §10 Switchboard mainnet IDs, an EXTERNAL audit of the final diff, and devnet runtime verification of this pass's banner/bounty changes (rescue reverted 2026-09-08). A dedicated reviewer agent was run but its sandbox blocked the skill companion files and it was cancelled mid-report — its partial notes corroborate the above guard list; treat the remaining deep-read gaps (staking full re-read, update_tradeday_stats sampling edge cases) as pre-existing checklist §14 territory, not new findings.

## 2026-09-07 audit pass #2 — value flows, oracle, staking (in-session, after checkout fix)

Scope covered directly (sub-agent fleet failed on every lane — output truncation ×3 + wall-time budget ×2 — so this pass was done by hand): `dex_buyback.rs`, `buy_the_dip.rs`, `calc_completed_offers.rs` decay, `update_tradeday_stats.rs`, staking `stake/unstake/claim/amm_stake` account constraints, crank-oracle authority paths, `offerMath.ts` mirror fidelity, sims (`run.py`, `run_dip.py`, `sheet_scenarios.py` re-run).

- [x] **Verified clean this pass:** dex_buyback day-budget accounting (slice ≤ remaining ⇒ no overflow; `bb_spent` bounded by snapshot; rollover via vault balance), pacing, vault-preview min-out with flat-floor fallback; buy_the_dip trigger/depth²/trend math (i64 clamp path), day-cap snapshot semantics, throttled self-sampling + cold-start dark; floor decay bounded (`cut.max(1).min(gap)` — floor lands on live, never below; fills can never block decay); `update_tradeday_stats` day-once + pool account pinning; staking vault address-pinning (S1) + index sequencing + last-staker penalty routing to bond vault; crank-oracle `set_bounty_amount`/`set_bounty_usd`/`set_authority` all `has_one = authority`; frontend `quoteEffectivePrice`/`quoteCostRaw` are an exact mirror of `quote_claim` across states 1/2 (checked branch-by-branch; the state-1 rescue was removed from BOTH sides 2026-09-08 — it had been UI-only, dead on-chain).
- [ ] **`test_set_state` permissionless — see §2 (elevated to hard launch blocker).**
- [x] **Keeper mainnet timezone inference (FIXED 2026-09-07 — both keepers).** `mev-keeper-mainnet.ts` and `mev-keeper.ts` `getSleepDuration` no longer infer ET from the host's UTC offset; both use `Intl.DateTimeFormat(..., { timeZone: "America/New_York", hour: "numeric", hourCycle: "h23" })` — EDT/EST handled by ICU, host-TZ-independent. No contract change needed (see §14).
- [ ] **Priority fees** — see §11 (keeper transition txs + claims carry no compute-unit-price).
- [ ] **Sim E2b failing (18/19): v3 daily-buyback trough.** `python3 sim/run.py --quiet` prints `min daily buyback ratio v3/v1: 0.31 (day 10)` on the rocket scenario (bar: ≥ 0.80) while v3 proceeds/token beat v1 by +2.61% — the deployed combinator compresses proceeds into fewer days and buys back less on the tail. Decide: accept (proceeds-optimal) or re-tune bump-taper/totals aggression. All 8/8 dip checks pass.
- [ ] **Econ: floor-held desk sells at ~spot (0–0.5% real discount) at night (state 2).** Observed on devnet 2026-09-07: claims priced at `floor − 0.5%×live` ≈ live with 260–560bps LISTED discounts. The UI is honest (real % shown, tone muted, "up to" pill), but a vesting-locked bond at ~spot is weak product value. Consider a minimum-real-discount guard (e.g. refuse sales with real discount < 50bps) or lean harder on decay until a real discount exists.

## 2026-09-08 pass — Solana network upgrades (SIMD-0525 slot times + SIMD-0296/0385 4096-byte v1 txs)

- [x] **Impact assessed (2026-09-08).** (1) Slot-time reduction — 400→350→300→250→200ms, 50ms/epoch steps, each step its own feature gate and gated on block-skip rates, targeted for Agave v4.2 — hits the compile-time pacing derivation directly (`MIN_SLICE_SLOTS`/`DIP_MIN_SLICE_SLOTS`/`SPOT_SAMPLE_SLOTS` = interval_ms / `NOMINAL_SLOT_MS`). Devnet measured TODAY: 360 slots/60s = **~167ms/slot** — devnet is already past the 400ms assumption, so devnet pacing is already ~2.4× the design cadence (≈25s/slice, dip ring every ≈12.5s); the keeper's 5-min ms/slot log prints the gap there now. (2) 4096-byte v1 transactions (SIMD-0385 format, SIMD-0296 size) are OPT-IN — v0/legacy keep working, no project program reads compute budget — so nothing breaks; they only unlock optional claim-path simplification. (3) Rent reduction: re-checked 2026-09-08 — the reduced rate is ALREADY live on devnet (~5,081 lamports/byte, down from the classic 6,960); mainnet sits at ~6,334/byte today and drops to the devnet rate with v4.2. 3-program deploy rent = ~7.0 SOL (devnet) / ~8.7 SOL (mainnet today). §12 updated with measured numbers.
- [x] **Slot-time deploy decision — DECIDED 2026-09-09: bake 200ms, `--final` immediately.** `NOMINAL_SLOT_MS = 200` (on-chain + both keeper mirrors) — the floor of the announced staged plan, so the upgrade authority can be revoked right away with no re-tune needed. The earlier "weekly 25/33.3ms decrement" idea was dropped: compile-time constant + immutability made it impossible, and the network's real curve is 50ms/epoch feature gates. Baked-200 consequence: while mainnet runs slower than 200ms, pacing runs SLOWER than design (400→120s/slice, 350→105s, 300→90s, 250→75s, 200→60s; hour-1 ≈ 36% of the day budget vs the ~50% design; unspent rolls over) — conservative, by construction. Devnet's ~167ms → ~50s/slice, slightly hot, bounded. Only sub-200ms chains would over-cadence vs design — outside every announced plan.
- [x] **Post-`--final` slot-time re-tuning — CLOSED OUT 2026-09-09, not built.** With 200ms baked (the announced floor), the authority-only state setter is unnecessary: over-cadence vs design would only occur below 200ms, and total spend is bounded by per-slice weights, the 5% band and day budgets at ANY slot time. Keeper mirrors are scripts — freely updateable if the constants ever need to diverge.
- [x] **Transition drift with the baked 200ms constant (2026-09-09).** Slice pacing = 300 slots = 60s@200 (design) / 75s@250 / 90s@300 / 105s@350 / 120s@400 (mainnet today); dip ring = 150 slots = 30s@200 / 60s@400; dip slice = same 300-slot rhythm. Until mainnet steps down, hour-1 front-loading runs BELOW the ~50% design (~36% at 400ms) — conservative; day budgets, per-slice weights and the 5% band are slot-count-independent and still bound total spend at any cadence. Devnet (~167ms) runs ~50s/slice — slightly hot, bounded. The keeper's measured-ms/slot log compares against 200 and will track each mainnet step as it lands.
- [x] **v1/4096 adapter IMPLEMENTED (2026-09-09) — thin adapter, no program change; remaining: live wallet + devnet verification.** `app/src/sdk/v1Transaction.ts` hand-rolls the SIMD-0385 wire format (spec-verified layout round-trip: version byte 129, header, config mask bits 0-1 priority fee u64 / bit 2 CU / bit 3 data size, addresses, headers, payloads, signatures-over-message-bytes; realistic 3-tier claim = 1,459 signed bytes). `useOfferClaim` builds one v1 tx for ALL SOL tiers (one wallet prompt), signs via the wallet's `signMessage` (byte signing — no wallet v1 support needed), dry-runs via raw RPC, and falls back to the v0+ALT path on any v1-specific failure; `VITE_V1_SOL_CLAIM=false` disables. Priority fees wired per the helius skill (estimate, never hardcode): `getPriorityFeeEstimate` (High) → recent-fees median fallback; injected as `setComputeUnitPrice` in the USDC + v0 SOL paths and as the v1 config total-lamports field (price × 400k CU, clamped 5k-20M lamports); the 1213-byte legacy SOL fallback has no room for a fee instruction (documented). `useDashData` ratchet reader bumped to `maxSupportedTransactionVersion: 1` with decode-failure skip. NOT yet verified live: wallet signMessage prompt UX, devnet v1 execution, `MaxLoadedAccountsDataSizeExceeded` headroom (64KiB limit set) — test on devnet before mainnet; v0+ALT fallback stays the safety net either way.
- [x] **v1 reader hardening (DONE 2026-09-09).** `useDashData.fetchRatchetHistory` now passes `maxSupportedTransactionVersion: 1` and skips signatures the 1.x SDK can't decode (the scanner only reads `meta.logMessages` — decay logs come from the keeper's legacy/v0 txs, so skipping v1 sigs loses nothing).

## 2026-09-09 pass — floor/live-price divergence investigation (devnet incident, evidence in `tools/diag-floor-price.mjs`)

- [ ] **`bounty_top_up` AFHO leg has no pool-depth/impact cap (launch-blocking economics — sizing HALF fixed 2026-09-10).** The top-up sizes its AFHO sell from the SOL price read (`afho_in = needed × sol_price × 1.0025² / afho_price`, `bounty_top_up.rs`) — nothing bounds it relative to the AFHO/USDC pool's depth, and there is no per-day frequency cap. Verified live on devnet 2026-09-09 under the OLD 0.4-SOL sizing: each transition paid the crank out of `bounty_vault`, and the next loop's top-up sold treasury AFHO into the thin pool — 39.4M AFHO for 101.56 USDC (**-20.1% in one tx**) and 112M AFHO for 198.33 USDC (**-41.0%**), against a pool whose USDC side was ~$658. **Fixed half:** the top-up is now sized to 10 bounty payments (≈ $7.50 at the $0.75 bounty) with a 10-payment low water — ~1.1% of that devnet pool's USDC side, inside the 5% depth rule, and self-scaling if `set-bounty-usd` changes the payment size. **Still open:** an explicit per-tx impact cap (≤5% of the AFHO/USDC pool's USDC side, mirroring the §5 wSOL-side rule) and a per-day top-up frequency cap as hard guards — the small size makes them comfortable rather than urgent, but they're cheap insurance for the mainnet canonical pool. Aggravator (a) (SOL/USDC drift inflating the sell size) is structurally reduced by the USDC denomination; aggravator (b) (crashes below `highest_buyback_basis` darkening the desk) is bounded by the smaller size but still real on thin pools.
- [ ] **M3 band blocks ratchets during fast rises — by design, but leaves the floor structurally behind after crashes.** `ratchet_within_band` requires `exec ≤ oracle×1.05` where the oracle side is the 600s TWAP. During the 21:37–21:51 external ramp (3228→8744, +5–15%/tx) every dex_buyback slice reverts `SlippageExceeded` (exec ≈ instant spot far above TWAP×1.05), so no fill ratchets while price is above the floor; slices only resume once price flattens (post-crash, below the floor, where fills can't ratchet). Net effect: the floor cannot follow spike-driven rises (correct — anti-spike, and the 22:04:43 -63% dump proved the spike was unsound to pin) but the desk reopens only via decay after each crash cycle. Revisit once the pool is deep: consider using the vault-ratio spot (not the TWAP) as the band's oracle side. (Investigation side-note, not a defect: the 21:37–21:51 ramp buys and the 22:04:43 123M-AFHO/652-USDC dump were EXTERNAL transactions — no amm program involvement.)
- [x] **Full-width stall banner (DONE 2026-09-09, `app/src/components/StaleDataBanner.tsx`).** Rendered in `Shell` on every page: when the shared snapshot has had no successful fetch for 10 minutes while the tab is visible, a fixed amber bar shows "Live updates stalled (N min)" + a Refresh button (invalidates the snapshot). Heartbeat = the snapshot's `dataUpdatedAt` (advances on every successful poll/WS-refetch, even when nothing changed on-chain). No manual dismiss — it vanishes on recovery; suppressed while a fetch is in flight and for 15s after the tab becomes visible (the enabled-flip recovery refetch must not flash it). KEPT for mainnet deliberately: the devnet-specific stall causes are gone (separate app RPC key, refetch/WS fixes), but every visitor tab shares the app's one public key (429 stretches under launch traffic) and provider outages are always possible — a frozen price with no indicator is how users attempt claims against stale state.
- [x] **Frontend refresh/robustness fixes (DONE 2026-09-09, `app/src`).** (1) `useDashData` ratchet history scan (30 txs × 900ms rate-limit sleeps ≈ 27s of serialized RPC) now `refetchOnWindowFocus: false` — react-query's default `true` re-ran the whole scan on every tab focus after 60s staleness, monopolizing the rate-limited endpoint and starving the snapshot/remaining queries into 429 backoff (the "old state shows" symptom). (2) `ChainDataProvider` account subscriptions stay subscribed across visibility flips (callbacks drop notifications while hidden) instead of tearing down ~10 WS subscriptions per tab switch; visibility-return recovery comes from the snapshot query's enabled-flip refetch. (3) `SiteNav` header ticker now shows the price source (`twap`/`spot`) + snapshot age and dims when stale (>60s) — the homepage price is the exact on-chain gating quantity (600s TWAP, vault-ratio fallback), and staleness is now visible instead of silent. `tsc -b` + `vite build` clean.
- [ ] **crank-oracle does not pin the Switchboard queue or feed id (launch-blocking trust gap).** `permissionless_crank` / `read_oracle_data` accept any quote account owned by the SBOD quote program whose PDA is canonical for the (queue, feed_ids) parsed out of its own blob — but never check that the queue is the expected one (`default_queue()` is referenced only in lib.rs tests) or that `feed_ids` contains the market-status feed; `BountyConfig` has no queue/feed fields. Anyone standing up their own SBOD queue + oracle can post arbitrary market states: cycle 1/2→0 to pump `trading_day_index` (early vesting/multiplier unlock), force halted (penalty griefing), or open/close the desk off-schedule; `last_updated_timestamp` resets on every forced transition (skews the buyback first-hour window). Compounding edge: `staleness = clock.slot − quote.slot` uses `saturating_sub`, so a quote carrying a FUTURE slot stays "fresh" indefinitely. Fix pre-`--final`: pin `queue` (and the status feed id) in `BountyConfig` at `initialize_bounty` and require equality in both quote-consuming instructions; `default_queue()` is already the intended mainnet value. (Found by a reviewer agent that then truncated before delivering its report — re-verified by hand in crank-oracle/src/lib.rs: quote checks at lines 455–470 / 566, config layout lines 124–140, default_queue usage lines 1375+.)


## 2026-09-10 pass — alt sheet (suspended-state second desk) + announcer hardening

- [x] **Alt sheet on-chain (DONE 2026-09-10, `programs/amm/src/instructions/alt_offers.rs`).** Fixed-terms second sheet live ONLY in state 3: `make_alt_offers` (authority‖keeper) posts exactly 5% of `afho_vault` — big 1% / med 1.5% / sml 2.5% + all rounding leftovers (bottom tier targets ~250 lots via the ladder; med/big ~30/~6 lots, tier ordering sml<med<big enforced); discounts fixed big −5% / med −4% / sml −3% OFF LIVE PRICE at claim time; vesting fixed 7/4/3 days; ONE sheet per trading day (`alt_list.day_index` cooldown). Deliberately a SEPARATE instruction family (`alt_offer_claim` / `alt_offer_claim_sol`) — no quote_claim exceptions: ratchet floor, late-nite boost, above-spot gate and fill-recording (metrics/demand-keep/ratchet) are all bypassed BY DESIGN, and the night-desk path stayed byte-identical. Claims hard-error `NotActive` on ANY 3→(0|1|2) transition (sheet goes dormant, revives if state 3 returns the same trading day; new day needs a fresh post). alt_list PDA (`b"alt_offer_list"`, mint) is created lazily via `init_if_needed` (fresh-detection via `owner == default` dodges the day-0 cooldown collision) so the live devnet deployment gets it without re-init. Payment splits 80/10/10 like the night desk; proceeds accumulate in `total_usdc_proceeds`. SOL leg mirrors offer_claim_sol exactly (solve → wrap → swap → fail-closed netting check). `alt_list` has NO close path (re-init impossible: program-owned PDA + no close instruction). Test: `tests/alt-sheet.test.ts` (8/8 on localnet: sizing math, cooldown, state gates, caller gates, lazy init, `PoolNotPinned` fail-closed claim).
- [x] **Easter-egg obfuscation (DONE 2026-09-10).** Nothing in the shipped frontend says "halt"/"fire sale": the banner copy is XOR+base64 in the bundle (`SpotlightBanner.tsx`, decode only in the state-3 render path; verified ciphertext-only in `dist/`), names are generic (`SpotlightBanner`, `data-desk='lit'`, `alt_*`), and CSS carries zero trigger words. Rust comments and this checklist DO say it (code/easter-egg boundary, per the user). The only readable "halted" strings in the bundle are the PRE-EXISTING market-status labels + the staking IDL penalty names — unchanged behavior.
- [x] **Lit desk UI (DONE 2026-09-10).** `data-desk='lit'` on the offer desk recolors existing per-tier effects to gold/amber/orange via the `--card-glow-*`/`--lit-*` custom props (no rule duplication), adds cornered backlit shadows on MORE elements (market badge, order bar, desk banner) via `litCorneredShadow`, and a violet/magenta full-width `SpotlightBanner` (sparse flicker-out, reduced-motion stripped, links to the desk, hidden ON the desk route) renders above every other page.
- [x] **Keeper →3 handler (DONE 2026-09-10, both `mev-keeper.ts` + `mev-keeper-mainnet.ts`).** Any →3 transition fires `make_alt_offers` (sim-first; "already posted today" simulation failure is expected noise on halt flaps, never kills the loop).
- [x] **Announcer (DONE 2026-09-10, `scripts/twitter-announcer.ts`).** (1) Market-state posts (incl. Monday open + the state-3 notice) are Telegram `disable_notification` silent — notable events keep pinging. (2) Decay digest moved from "first poll ≥ noon" to a RANDOM minute inside 12:00–13:00 ET, re-rolled daily (`rollDecaySlotMinute`; same once-per-day latch + baseline logic). (3) Alt-sheet announcement (`altSheetMessage`): one per trading day, scheduled exactly 5 min after the window opens, and ONLY fires if the window is still open at post time — no end-of-window post, ever; copy is oblique (no state names, no sale jargon), tier lines carry the concrete terms; mid-window restart seeds silently (never re-announces).
- [ ] **Alt-sheet claim e2e on devnet (pre-mainnet rehearsal).** Localnet covers everything except real CPMM pricing (no local Raydium). During the Phase-0/1 rehearsal: drive a test halt, let the keeper post, claim one tier in each currency, verify the 3–5% pricing vs pool spot and the 80/10/10 splits, then verify the sheet retires on halt lift.
- [ ] **Alt-sheet + halt-day interaction with the daily desk (design note, decide before mainnet).** A halt sale does NOT consume or reset the regular sheet: the regular sheet survives under the hood (claims are DeskClosed-blocked in state 3 anyway) and the day-end 3→2 still posts a fresh regular sheet. A same-day halt flap revives the REMAINING alt lots (state check only) — intended, but worth a conscious sign-off. The alt sheet also has no minimum-halt-duration guard on-chain (the dual-source feed job + the keeper's poll cadence + the 5-min announcement delay are the glitch defenses); if a deeper guard is wanted, add a keeper-side "state 3 seen on N consecutive polls" wait.
